torch>=2.0.0
torchvision>=0.15.0
ultralytics>=8.0.0
numpy>=1.23.0
Pillow>=9.0.0
pyyaml>=6.0
opencv-python>=4.6.0
openvino>=2023.4
fastapi>=0.110.0
uvicorn>=0.23.0
python-multipart>=0.0.6
pydantic>=1.10.0,<3.0.0
psutil>=5.9.0

# Notes:
# - If you use a GPU build of PyTorch, install the matching torch+cu### wheel from https://pytorch.org
# - ultralytics may require extra dependencies (like supervisely or export tools); pin the version you used during development
# - openvino package name and versioning can vary by platform; if you use OpenVINO toolkit, prefer vendor instructions
